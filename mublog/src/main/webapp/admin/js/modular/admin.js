'use strict';

var admin = angular.module('admin',['ngRoute','ngResource']);
/*admin.config(function($routeProvider){
    $routeProvider
        .when('/article',{
            templateUrl: '/views/admin/article.jsp',
            controller: 'controller/ArticleController'
        })
        .otherwise(
            { redirectTo: '/'}
        );
});*/



